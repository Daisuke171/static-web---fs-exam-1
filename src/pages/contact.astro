---
import MainLayout from '../layout/MainLayout.astro';
---

<script type="module" is:inline>
  document.addEventListener('DOMContentLoaded', () => {
    const $form = document.querySelector("#contact-form");
    const $inputs = $form.querySelectorAll("input");
    const $inputNumber = $form.querySelector('input[name="number"]');
    const $errorMsg = document.getElementById("error-msg");

    // Validación visual del "número"
    $inputNumber.addEventListener('input', () => {
      $inputNumber.style.backgroundColor = $inputNumber.value.length < 10 ? 'red' : 'grey';
    });

    // Validación al enviar el formulario
    $form.addEventListener("submit", (e) => {
      e.preventDefault();

      const vacio = Array.from($inputs).some(input => {
        const esVacio = input.value.trim() === "";
        input.style.border = esVacio ? "2px solid red" : "rgba(255, 255, 255, 0.4);";
        return esVacio;
      });

      if (vacio) {
        $errorMsg.style.display = "block";
      } else {
        $errorMsg.style.display = "none";

        const datos = Object.fromEntries(
          Array.from($inputs).map(input => [input.name, input.value])
        );

        console.log("Datos del formulario:", datos);
      }
    });
  });
</script>

    



<MainLayout title="Movie Zone">

  <section class="contect-form">
    <form class="contect-form__contact" id="contact-form" action="">

      <h2 class="contect-form__title">Registro</h2>

      <label class="contect-form__name"  for="nombre">Nombre</label>
      <input class="contect-form__name-in" type="text" id="name" name="name" >

      <label for="apellido" class="contect-form__name"  >Apellido</label>
      <input  class="contect-form__name-in" type="text" id="apellido" name="last-name">
     
      <label for="email" class="contect-form__name" >Email</label>
      <input  class="contect-form__name-in" type="email" id="email" name="email">

      <label for="numero" class="contect-form__name"  >Numero de telefono</label>
      <input  class="contect-form__name-in" type="number" id="numero" name="number">
      
       <label for="subject " class="contect-form__name">Asunto</label>
       <input class="contect-form__name-in" type="text" id="subject" name="subject" />

 
      
      <button class="contect-form__button" type="submit" id="" name="boton">Enviar</button>
      <p id="error-msg" class="contect-form__p">Por favor, completá todos los campos.</p>
    </form>

    
  </section>

   <footer class="footer">
    <nav class="footer-nav">
      <a href="main/Homepage" class="footer-nav__link"> Volver a pagina principal </a>
    </nav>
  </footer>


</MainLayout>

<style>

html, body{
  font-weight: var(--font-weight-bold);
  font-family: var(--font-main);
   background: var(--gradient-primary);
   display: flex;
   flex-direction: column;
   justify-content:center;
   gap: 1rem;
   height:100%;

}

.contect-form__title{
  color:var(--color-accent);
  font-size:1.2rem;
}

.contect-form {
  width: 100%;
  height: 90%;
  display: flex;
  justify-content: center;
}

.contect-form__contact { 
  background: white;
  display: flex;
  flex-direction: column;
  width: 80%;
  gap: 0.2rem;
  padding:2rem;
  border-radius: 2rem;
  justify-content: center;
  height: 90%;
}

.contect-form__name {
  font-size: 0.8rem;
  margin:auto;
  width:350px;
}

.contect-form__name-in {
    margin: auto;
    width:400px;
    padding: 0.5rem;
    font-size: 0.8rem;
    border-radius: 20px;
    border: 1px solid rgba(255, 255, 255, 0.4);
    background: rgb(191, 202, 198) ;
}


.contect-form__button{
 width: fit-content;
    padding: 0.7em 2em;
    border-radius: 20px;
    border: none;
    background-color: var(--color-accent);
    color: var(--color-dark);
    cursor: pointer;
    transition: all 0.3s ease;
    font-size: 0.8rem;
    margin: auto;
}

.contect-form__p{
  color: red;
  display: none;
  font-zise: 0.5rem;
  }

  .footer {
    background: var(--color-dark);
    color: var(--color-light);
    padding: 2rem 1rem;
    display: flex;
    justify-content: center;
    align-items: center;
    border-top: 2px solid var(--color-primary);
    position: fixed;
    bottom: 0;
    }

  .footer-nav {
    display: flex;
    gap: 1.2rem;
    align-items: center;
    font-family: var(--font-main);
    font-size:0.8rem;
    height: 0.2rem;
  }

  .footer-nav__link {
    color: var(--color-accent);
    text-decoration: none;
    font-weight: var(--font-weight-medium);
    transition: color 0.3s ease;
  }

  .footer-nav__link:hover {
    color: var(--color-light);
  }



@media screen and (min-width: 680px){

  .contect-form__title{
  color:var(--color-accent);
  font-size:1.5rem;
}

.contect-form__contact { 
  width: 50%;
  gap: 0.3rem;
  padding:3rem;
  height: 90%;
}

.contect-form__name {
  font-size: 0.9rem;
}

.contect-form__name-in {
    margin: auto;
    width:400px;
    padding: 0.5rem;
    font-size: 1rem;
}

}



@media screen and (min-width: 1240px){

.contect-form__title{
  color:var(--color-accent);
  font-size:2rem;
  
}

.contect-form__name {
  font-size: 1rem;
}

  .contect-form__contact { 
  width: 30%;
  gap: 0.3rem;
  padding:3rem;
  height: 90%;
}


.contect-form__name-in {
    margin: auto;
    width:400px;
    padding: 0.5rem;
    font-size: 1.2rem;
}

.contect-form__p{
  font-zise: 1rem;
}


 .footer-nav {
    font-size: 1rem;
    height: 1.2rem;
  }

  .contect-form__button{
 
    padding: 0.8em 2em;
    font-size: 0.9rem;
    margin-top: 0.2rem;
  
}

}
  

</style>
