---

---

<script is:client>
  document.addEventListener("DOMContentLoaded", () => {
    const toggleBtn = document.getElementById("menu-toggle");
    const menu = document.getElementById("nav-menu");

    if (toggleBtn && menu) {
      toggleBtn.addEventListener("click", () => {
        menu.classList.toggle("is-hidden");
      });
    }
  });
</script>


    <header class="favorites__header">
         <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
        <nav>
            <div class="favorites__header__left">
              <button id="menu-toggle" class="favorites__header__left--menuButton" aria-label="Abrir menú">
                     <i class="fa fa-bars"></i>
               </button>
                <a href="" class="favorites__header__left--logo" data-astro-reload>
                <a href="/main/Homepage" class="favorites__header__left--logo" data-astro-reload>
                    <figure>
                        <img src="/images/logo.png" alt="">
                    </figure>
                </a>
            </div>


            <div class="favorites__header__center is-hidden" id="nav-menu">
            <div class="favorites__header__center--home">
                <a href="/main/Homepage" data-astro-reload>Inicio</a>
           </div>
            <div class="favorites__header__center--moviesButton">
                 <a href="/movies/1" data-astro-reload>Peliculas</a>
           </div>
            <div class="favorites__header__center--actorsButton">
                  <a href="/actors/1" data-astro-reload>Actores</a>
            </div>
            </div>   
            <div class="favorites__header__right">
                

                <div class="favorites__header__right--profileButton">
                    <a href="../user" class="favorites__header__right--profileButton" data-astro-reload>
                        <figure>
                            <img src="/images/placegolderAvatar.png" alt="">
                        </figure>
                    </a>
                </div>
            </div>
        </nav>
</header>

<style>
  .favorites__header {
  background: oklch(0.1751 0.0074 103 / 53.1%);
  padding: 0.1rem 0;
  color: #fff;
  font-weight: var(--font-weight-bold);
  font-family: var(--font-main);
}

.favorites__header nav {
  display: flex;
  flex-direction:row; /* En columna en móvil */
  padding: 0 1rem;
  gap: 1rem;
  justify-content: center;
  align-items: center;
}

.favorites__header__left,
.favorites__header__center,
.favorites__header__right {
  display: flex;
  align-items: center;
  width: fit-content;
  justify-content: center;
}

.favorites__header__right--profileButton img {
  width: 30px;
  height: 30px;
  min-width: 30px;
  min-height: 30px;
  border-radius: 50%;
}

.favorites__header__left {
  gap: 0;
  position: relative;
  right: 2rem;
}

.favorites__header__left--logo img {
  width: 200px;
  height: 100px;
  position: right;
  right: 1rem;
}

.favorites__header__left--menuButton,
.favorites__header__center--moviesButton a,
.favorites__header__center--actorsButton a,
.favorites__header__center--home a,
.favorites__header__right--searchButton i {
  color: #fff;
  text-decoration: none;
  margin-right: 1rem;
  font-size: 1.1rem;
  transition: color 0.2s;
}

/* Mostrar el botón de menú en móvil */
.favorites__header__left--menuButton {
  display: block;
  background: none;
  border: none;
  cursor: pointer;
}

  /* Mostrar menú por defecto en escritorio */
  #nav-menu {
    display: flex;
    gap: 1rem;
  }

   

  /* Responsive: ocultar en móviles */
  @media (max-width: 768px) {
    .favorites__header__left--menuButton {
      display: block;
    }

    #nav-menu {
      display: none;
      flex-direction: column;
      position: absolute;
      top: 70px;
      left: 0;
      width: 100%;
      background: #222;
      padding: 1rem;
      z-index: 999;
    }

#nav-menu a {
  margin: 0.5rem 0;
  color: white;
}

#nav-menu.is-hidden {
  display: none;
}

#nav-menu:not(.is-hidden) {
  display: flex;
}

/* TABLET: desde 600px */
@media (min-width: 600px) {
  .favorites__header nav {
    flex-direction: row;
    justify-content: space-between;
  }

  .favorites__header__left--logo img {
    width: 250px;
    right: 4rem;
  }

  .favorites__header__left {
    right: 2rem;
  }

    
  }

 @media screen and (max-width: 380px) {
    .favorites__header__left--logo img{
      right: 8rem;
    }
  }

  @media (min-width: 780px) {
    .favorites__header__center {
      display: flex !important;
      position: static;
      background: none;
      flex-direction: row;
      padding: 0;
    }

    .favorites__header__left--menuButton {
      display: none;
    }
  }

@media screen and (min-width: 769px){
   #nav-menu{
    position: relative;
    right: 3rem;
    gap: 4rem;
  }
}
@media screen and (min-width: 1080px){
   #nav-menu{
    position: relative;
    right: 4rem;
  }

  .favorites__header__left--logo img {
    width: 300px;
    right: 5rem;
  }

  .favorites__header__left {
    right: 3rem;
  }

  .favorites__header__center a {
    font-size: 1.2rem;
    margin-right: 2rem;
  }
}

</style>









